server {
  listen 80;
  server_name maximcolls.com;
  root /var/www/maxim-colls/public;

  location = /nginx_stub_status {
    stub_status on;
    auth_basic "nginx status";
    auth_basic_user_file /var/www/.htpasswd;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
